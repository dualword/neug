name: Build Nightly Wheels

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  workflow_dispatch:
    inputs:
      python_versions:
        description: 'Python version (e.g. 3.9) or range (e.g. 3.8-3.14) to build'
        required: false
        default: ''
  schedule:
    # Trigger the workflow at 03:00(CST) every day (19:00 UTC)
    - cron: '00 19 * * *'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/build-nightly-wheel.yml'

concurrency:
  group: ${{ github.repository }}-${{ github.event.number || github.head_ref || github.sha }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build_wheels:
    uses: ./.github/workflows/wheels-common.yml
    with:
      python_versions: ${{ inputs.python_versions }}
      mode: nightly
      set_nightly_version: true
      pybind11_findpython_off: false
      linux_x86_python: '3.13'
      linux_arm_python: '3.13'
      mac_python: '3.13'
      cibuildwheel_version: '3.4.0'
      enable_macos_x86: false
      caller_event_name: ${{ github.event_name }}
    secrets: inherit
